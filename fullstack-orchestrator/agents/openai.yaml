interface:
  display_name: "Fullstack Orchestrator"
  short_description: "Compose behavior, conduct delivery, verify gates"
  default_prompt: "Use $fullstack-orchestrator to compose the behavior contract, conduct delivery across project surfaces, and verify every gate with evidence."
