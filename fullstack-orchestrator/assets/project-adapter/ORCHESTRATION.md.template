# {project_name} Orchestration

This file contains the approved project topology for orchestration. This
orchestration repo owns shared project routing; implementation repos are
surfaces only.

## Repositories

| Alias | Role | Local path | Remote | Default branch | Owner | Notes |
| --- | --- | --- | --- | --- | --- | --- |
{repo_rows}

## Branch And Worktree Policy

See `WORKTREES.md` for task checkout policy, canonical pickup branches,
cross-repo landing, and cleanup.

## Landing Policy

- For single-repo work, land after that repo passes its documented gates.
- For cross-repo work, use all-or-hold landing unless a slice explicitly says
  otherwise.

## Deploy Authority

- Deployment requires an explicit deploy trigger from the user or this project's
  `DEPLOY.md`.
